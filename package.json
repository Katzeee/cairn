{
  "name": "cairn-workspace",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0 <27.0.0"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@cairn/design-tokens && npm run build --workspace=@cairn/design-system-catalog && npm run build --workspace=@cairn/ui && npm run build --workspace=@cairn/showcase",
    "showcase": "npm run build && npm run preview --workspace=@cairn/showcase",
    "test": "npm run build && npm run verify:catalog && npm run verify:entries && npm run test --workspaces --if-present",
    "verify:catalog": "node scripts/verify-catalog.mjs",
    "verify:entries": "node scripts/verify-entry-boundaries.mjs",
    "typecheck": "npm run build && npm run typecheck --workspaces --if-present && npm run typecheck:test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^20.19.43",
    "axe-core": "^4.13.0",
    "electron": "41.10.7",
    "esbuild": "0.28.2",
    "eslint": "^10.4.1",
    "eslint-config-prettier": "^10.1.8",
    "playwright-core": "1.62.1",
    "stylelint": "^17.15.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.60.1",
    "vitest": "^4.1.11"
  },
  "allowScripts": {
    "@parcel/watcher@2.5.1": true,
    "electron@41.10.7": true,
    "esbuild@0.28.2": true
  }
}
